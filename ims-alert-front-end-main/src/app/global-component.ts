export const GlobalComponent = {
    // Api Calling
    // API_URL : 'http://localhost:8086/api/',
    // headerToken : {'Authorization': `Bearer ${localStorage.getItem('auth-token')}`},

    // // Auth Api
    // AUTH_API:"http://localhost:8086/api/auth/",

    // COMPANY_ID:1
   
}